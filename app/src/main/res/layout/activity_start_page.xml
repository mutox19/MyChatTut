<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/start_page"
    tools:context="com.example.danilo.mychattut.StartPageActivity">

    <Button
        android:id="@+id/alreadyHaveAccountBtn"
        android:layout_width="320dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="188dp"
        android:background="@android:color/background_light"
        android:padding="10dp"
        android:text="Already Have An Account?"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="20sp"
        tools:text="Already Have An Account?" />



    <Button
        android:id="@+id/needAccountBtn"
        android:layout_width="320dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="106dp"
        android:background="@android:color/background_light"
        android:padding="10dp"
        android:text="Need New Account?"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="20sp"
        tools:text="Need New Account?" />
</RelativeLayout>
